{{!--
This header template is shared across all the pages.
--}}

{{#if background}}
<header class="m-header with-picture js-header">
  {{else}}
  <header class="m-header js-header">
    {{/if}}
    <div class="m-mobile-topbar" data-aos="fade-down">
      <button class="m-icon-button in-mobile-topbar js-open-menu" aria-label="Open menu">
        <span class="icon-menu"></span>
      </button>
      {{#if @site.logo}}
      <a href="{{@site.url}}" class="m-logo in-mobile-topbar translateMe_navLogo">
        <img src="{{@site.logo}}" alt="{{@site.title}}">
      </a>
      {{else}}
      <a href="{{@site.url}}" class="m-site-name in-mobile-topbar">
        {{@site.title}}
      </a>
    {{/if}}
        <button class="m-icon-button in-mobile-topbar js-open-search" aria-label="Open search">
      <span class="icon-search"></span>
    </button>
  </div>
  <div class="m-menu js-menu">
    <button class="m-icon-button outlined as-close-menu js-close-menu" aria-label="Close menu">
      <span class="icon-close"></span>
    </button>
    <div class="m-menu__main" data-aos="fade-down">
      <div class="l-wrapper">
        <div class="m-nav">
          <nav class="m-nav__left" aria-label="busrides-navigation">
            {{#if @site.navigation}}
              {{navigation}}
              {{/if}}
            </nav>
            <nav class="m-nav__right">
              <ul role="menu">
                <li>
                  <button class="m-icon-button in-menu-main js-open-search" aria-label="Open search">
                    <span class="icon-search"></span>
                  </button>
                </li>
                <li id="nav-english" class="nav-english translateMe_navItem">
                  <a href="/">EN</a>
                </li>
                <li id="nav-francais" class="nav-francais translateMe_navItem">
                  <a href="/fr/">FR</a>
                </li>
              </ul>
            </nav>
          </div>
        </div>
      </div>
    </div>
  </header>
<!-- Beta Header -->
{{> beta-feedback }}
<!-- Beta Header -->