<div>
    <ul class="nav">
        <!-- Loop through the navigation items -->
        {{#foreach navigation}}
        {{createNavItem slug current url label}}
        <h1>TEST</h1>
        {{!-- <li class="nav-{{slug}}{{#if current}} nav-current{{/if}}"><a href="{{url absolute="true"}}">{{label}}</a></li> --}}
        {{/foreach}}
        <!-- End the loop -->
    </ul>
</div>